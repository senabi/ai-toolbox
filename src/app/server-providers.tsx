// import { cookies } from "next/headers";
// import { TRPCReactProvider } from "@/trpc/react";
import type { PropsWithChildren } from "react";

export function ServerProviders(props: PropsWithChildren) {
  return (
      props.children
  );
}
